#include "filedownloader.h"

int FileDownloader::fetchFile(std::string filename, std::string saveAs) {
    printf("File fetching not implemented, missing file %s\n", saveAs.c_str());
    return -1;
    // TODO: implement
}
